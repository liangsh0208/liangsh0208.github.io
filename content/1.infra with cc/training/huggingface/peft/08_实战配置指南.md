# 08_实战配置指南

## 文档元信息

| 属性 | 内容 |
|------|------|
| 适用版本 | PEFT >= 0.7.0 |
| 测试环境 | Python 3.10+, PyTorch 2.0+, Transformers 4.35+ |
| 硬件要求 | 根据配置从 8GB 到 40GB+ GPU |

---

## 1. 标准 LoRA 配置

### 1.1 基础配置

```python
import torch
from transformers import (
    AutoModelForCausalLM,
    AutoTokenizer,
    TrainingArguments,
    Trainer,
    DataCollatorForLanguageModeling
)
from peft import LoraConfig, get_peft_model, TaskType

# 1. 加载模型和分词器
model_id = "Qwen/Qwen2.5-3B-Instruct"
model = AutoModelForCausalLM.from_pretrained(
    model_id,
    torch_dtype=torch.float16,
    device_map="auto"
)
tokenizer = AutoTokenizer.from_pretrained(model_id)

# 2. 配置 LoRA
peft_config = LoraConfig(
    task_type=TaskType.CAUSAL_LM,
    r=16,                          # 低秩维度
    lora_alpha=32,                 # 缩放系数 (通常 = 2 * r)
    lora_dropout=0.05,             # Dropout 率
    target_modules=[               # 目标模块
        "q_proj", "k_proj", "v_proj", "o_proj",
        "gate_proj", "up_proj", "down_proj"
    ],
    bias="none",
    use_rslora=False,              # 是否使用 RS-LoRA
)

# 3. 创建 PEFT 模型
model = get_peft_model(model, peft_config)

# 4. 查看可训练参数
model.print_trainable_parameters()
# 输出: trainable params: 9,437,184 || all params: 2,994,922,752 || trainable%: 0.3151
```

### 1.2 训练参数

```python
# 训练参数配置
training_args = TrainingArguments(
    output_dir="./lora_output",
    overwrite_output_dir=True,
    num_train_epochs=3,
    per_device_train_batch_size=4,
    gradient_accumulation_steps=4,
    learning_rate=1e-4,              # LoRA 可用较高学习率
    warmup_steps=100,
    weight_decay=0.01,
    logging_steps=10,
    save_strategy="epoch",
    fp16=True,
    gradient_checkpointing=True,
)

# 创建 Trainer
trainer = Trainer(
    model=model,
    args=training_args,
    train_dataset=train_dataset,
    data_collator=DataCollatorForLanguageModeling(tokenizer, mlm=False),
)

# 训练
trainer.train()

# 保存 adapter
model.save_pretrained("./qwen_lora_adapter")
```

---

## 2. QLoRA 配置

### 2.1 4-bit 量化训练

```python
from transformers import BitsAndBytesConfig
from peft import prepare_model_for_kbit_training

# 1. 配置 4-bit 量化
bnb_config = BitsAndBytesConfig(
    load_in_4bit=True,
    bnb_4bit_quant_type="nf4",          # Normal Float 4
    bnb_4bit_compute_dtype=torch.float16,
    bnb_4bit_use_double_quant=True,     # 双重量化
)

# 2. 加载量化模型
model = AutoModelForCausalLM.from_pretrained(
    "Qwen/Qwen2.5-7B-Instruct",
    quantization_config=bnb_config,
    device_map="auto",
    low_cpu_mem_usage=True,
)

# 3. 准备模型用于训练
model = prepare_model_for_kbit_training(model)

# 4. 配置 LoRA
lora_config = LoraConfig(
    r=64,
    lora_alpha=16,
    target_modules="all-linear",          # 自动选择所有线性层
    lora_dropout=0.1,
    bias="none",
    task_type="CAUSAL_LM",
    use_gradient_checkpointing="unsloth", # 扩展梯度检查点
)

model = get_peft_model(model, lora_config)

# 5. 分页优化器（避免 OOM）
from bitsandbytes.optim import PagedAdamW8bit

optimizer = PagedAdamW8bit(
    model.parameters(),
    lr=1e-4,
    betas=(0.9, 0.999),
)
```

### 2.2 使用 trl 简化训练

```python
from trl import SFTTrainer
from peft import LoraConfig

# 更简洁的 QLoRA 训练
lora_config = LoraConfig(
    r=64,
    lora_alpha=16,
    target_modules="all-linear",
    lora_dropout=0.05,
    bias="none",
)

trainer = SFTTrainer(
    model=model,
    tokenizer=tokenizer,
    train_dataset=dataset,
    peft_config=lora_config,
    dataset_text_field="text",
    max_seq_length=512,
    args=TrainingArguments(
        per_device_train_batch_size=1,
        gradient_accumulation_steps=4,
        num_train_epochs=3,
        learning_rate=2e-4,
        fp16=True,
        output_dir="./results",
    ),
)

trainer.train()
```

---

## 3. DoRA 配置

### 3.1 Weight-Decomposed LoRA

```python
# DoRA: 幅度与方向解耦，低 rank 效果更好
lora_config = LoraConfig(
    r=8,                      # 可用更小的 rank
    lora_alpha=16,
    target_modules=["q_proj", "v_proj"],
    use_dora=True,            # 启用 DoRA
    lora_dropout=0.0,
)

model = get_peft_model(model, lora_config)
```

---

## 4. 多任务 Adapter 管理

### 4.1 加载多个 Adapter

```python
from peft import PeftModel

# 1. 加载基础模型
base_model = AutoModelForCausalLM.from_pretrained("meta-llama/Llama-2-7b")

# 2. 装载第一个 adapter
model = PeftModel.from_pretrained(base_model, "user/lora-translation")

# 3. 加载其他 adapters
model.load_adapter("user/lora-summarization", adapter_name="summarize")
model.load_adapter("user/lora-coding", adapter_name="coding")

# 4. 切换 adapter
def generate_with_adapter(model, adapter_name, prompt):
    model.set_adapter(adapter_name)
    model.eval()
    inputs = tokenizer(prompt, return_tensors="pt").to(model.device)
    outputs = model.generate(**inputs, max_new_tokens=200)
    return tokenizer.decode(outputs[0])

# 使用不同 adapter
translation = generate_with_adapter(model, "default", "Translate: Hello")
summary = generate_with_adapter(model, "summarize", "Summarize: ...")
```

---

## 5. 配置方案对比

### 5.1 场景选择

| 场景 | 配置 | 显存需求 | 推荐 rank |
|------|------|----------|----------|
| **7B 模型全参数** | 无 PEFT | 40GB+ | - |
| **7B LoRA** | r=16, target=[q,v] | 24GB | 8-32 |
| **7B QLoRA** | 4-bit + r=64 | 12GB | 32-128 |
| **13B QLoRA** | 4-bit + r=64 | 20GB | 32-128 |
| **70B QLoRA** | 4-bit + r=256 | 40GB | 128-512 |

### 5.2 初始化方法选择

| 方法 | 配置 | 适用场景 |
|------|------|----------|
| **默认** | `init_lora_weights=True` | 通用场景 |
| **PiSSA** | `init_lora_weights="pissa"` | 追求更快收敛 |
| **OLoRA** | `init_lora_weights="olora"` | 配合 QLoRA |
| **EVA** | `init_lora_weights="eva"` | 数据驱动初始化 |

---

## 6. 常见训练脚本模板

### 6.1 完整 LoRA 脚本

```python
#!/usr/bin/env python
"""标准 LoRA 微调脚本"""
import torch
from datasets import load_dataset
from transformers import (
    AutoModelForCausalLM,
    AutoTokenizer,
    TrainingArguments,
    Trainer,
    DataCollatorForSeq2Seq,
)
from peft import LoraConfig, get_peft_model, TaskType

# ============ 配置 ============
MODEL_NAME = "Qwen/Qwen2.5-3B-Instruct"
DATASET_NAME = "your_dataset"
OUTPUT_DIR = "./lora_output"

LORA_R = 16
LORA_ALPHA = 32
LORA_DROPOUT = 0.05
TARGET_MODULES = ["q_proj", "v_proj"]

LEARNING_RATE = 1e-4
BATCH_SIZE = 4
EPOCHS = 3

# ============ 加载模型 ============
model = AutoModelForCausalLM.from_pretrained(
    MODEL_NAME,
    torch_dtype=torch.float16,
    device_map="auto",
)
tokenizer = AutoTokenizer.from_pretrained(MODEL_NAME)

# ============ 配置 LoRA ============
lora_config = LoraConfig(
    task_type=TaskType.CAUSAL_LM,
    r=LORA_R,
    lora_alpha=LORA_ALPHA,
    lora_dropout=LORA_DROPOUT,
    target_modules=TARGET_MODULES,
)

model = get_peft_model(model, lora_config)
model.print_trainable_parameters()

# ============ 加载数据 ============
dataset = load_dataset(DATASET_NAME, split="train")

def preprocess(examples):
    # 根据实际数据格式调整
    texts = [f"{p}\n{a}" for p, a in zip(examples["prompt"], examples["response"])]
    return tokenizer(texts, truncation=True, max_length=512, padding="max_length")

tokenized_dataset = dataset.map(preprocess, batched=True)

# ============ 训练 ============
training_args = TrainingArguments(
    output_dir=OUTPUT_DIR,
    num_train_epochs=EPOCHS,
    per_device_train_batch_size=BATCH_SIZE,
    learning_rate=LEARNING_RATE,
    warmup_steps=100,
    logging_steps=10,
    save_strategy="epoch",
    fp16=True,
)

trainer = Trainer(
    model=model,
    args=training_args,
    train_dataset=tokenized_dataset,
    data_collator=DataCollatorForSeq2Seq(tokenizer, pad_to_multiple_of=8),
)

trainer.train()
model.save_pretrained(OUTPUT_DIR)
```

---

## 7. 排错指南

### 7.1 Loss 不下降

```python
# 1. 检查 target_modules
print(model)  # 确认 LoRA 层已注入

# 2. 增大学习率
learning_rate = 2e-4  # 试试更大的

# 3. 检查数据格式
# 确保 label 正确设置
```

### 7.2 OOM 错误

```python
# 1. 启用梯度检查点
model.gradient_checkpointing_enable()
model.enable_input_require_grads()

# 2. 减小 batch size
per_device_train_batch_size = 1
gradient_accumulation_steps = 8  # 累积梯度

# 3. 使用 QLoRA
```

### 7.3 推理结果异常

```python
# 1. 确保推理模式
model.eval()

# 2. 检查 adapter 加载
model.set_adapter("your_adapter")  # 明确指定

# 3. 合并后测试
merged_model = model.merge_and_unload()
```

---

## 相关文档

- [02_LoRA实现详解.md](02_LoRA实现详解.md) - LoRA 原理
- [03_QLoRA量化训练.md](03_QLoRA量化训练.md) - 量化训练
- [07_模型层映射表.md](07_模型层映射表.md) - target_modules 参考

---

*文档生成日期: 2026-04-20*
